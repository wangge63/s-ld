server:
  port: 1105
spring:
  datasource:
    type:
    url: jdbc:mysql://localhost:3306/sld?serverTimezone=UTC
    username: root
    password: mac_mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    open-in-view: false
